const RESPONSE_HEADERS = Object.freeze({
  partialDownload: 'X-SendaDocs-Partial-Download',
  omittedCount: 'X-SendaDocs-Omitted-Count',
  omittedItems: 'X-SendaDocs-Omitted-Items',
});

module.exports = {
  RESPONSE_HEADERS,
};
